package cp620.content.api;

public final class HookConst
{
    // For int-return hooks (e.g., acceptStack). Anything else is treated as "decided".
    public static final int PassInt = Integer.MIN_VALUE;

    private HookConst()
    {
    }
}
